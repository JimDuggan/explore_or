# Four common types
# logical
# integer
# double (or numeric)
# character
# Usually created with c() – short for combine

x_int <- c(10L, 20L, 30L)
x_num <- c(12, 45.5, 67)
x_log <- c(TRUE, FALSE, TRUE)
x_str <- c("CT561", "CT5102", "CT101")

# Coercion of vectors

x_new <- c(x_int,x_log)
x_new1 <- c(x_str,x_log)






